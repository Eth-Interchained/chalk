{
  "name": "chalk-engine",
  "version": "0.2.0",
  "description": "CHALK — local-first football intelligence engine behind sports-rater.com. The database knows. Deterministic code calculates. The model interprets. Provenance proves.",
  "type": "module",
  "license": "BUSL-1.1",
  "engines": { "node": ">=24" },
  "bin": { "chalk": "./bin/chalk.ts" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/*.test.ts\"",
    "ingest": "node bin/chalk.ts ingest",
    "serve": "node bin/chalk.ts serve"
  },
  "dependencies": {
    "nedb-engine": "^2.8.2",
    "nedb-engine-client": "^2.8.2",
    "sentinel-blocks": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
