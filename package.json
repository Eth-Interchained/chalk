{
  "name": "chalk-engine",
  "version": "0.10.1",
  "description": "CHALK \u2014 local-first football intelligence engine behind sports-rater.com. The database knows. Deterministic code calculates. The model interprets. Provenance proves.",
  "type": "module",
  "license": "BUSL-1.1",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "chalk": "./bin/chalk.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"tests/*.test.ts\" && CHALK_TEST_STORE=embedded node --test \"tests/*.test.ts\"",
    "test:http": "node --test \"tests/*.test.ts\"",
    "test:embedded": "CHALK_TEST_STORE=embedded node --test \"tests/*.test.ts\"",
    "ingest": "node bin/chalk.ts ingest",
    "serve": "node bin/chalk.ts serve"
  },
  "dependencies": {
    "nedb-engine": "^2.8.4",
    "nedb-engine-client": "^2.8.4",
    "sentinel-blocks": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  },
  "author": "Interchained LLC <founders@vibecode-101.com> (https://sports-rater.com)",
  "copyright": "Copyright (c) 2026 Interchained LLC"
}
